# AudioTool — Backlog

> **Single Source aller offenen Arbeiten.** Zwei Top-Level-Blöcke: **[Noch Offen](#noch-offen)** (alle ausstehenden Arbeiten) und **[Erledigt](#erledigt)** (Ledger der abgeschlossenen Aufgaben, ganz unten). Innerhalb von „Noch Offen" bleibt die gewohnte Gliederung: **Teil A** = Release-Härtung aus dem Code-Review, **Teil B** = geplante Features & Roadmap. Wissen, Architektur und Designentscheidungen leben in [`CLAUDE.md`](CLAUDE.md).
> **Reihenfolge = Priorität** (innerhalb der Abschnitte). Zeilennummern driften; im Zweifel über Symbol/Methodenname suchen.
>
> **Status-Konvention (einheitlich):** Offene Aufgaben stehen unter „Noch Offen" mit leerer Checkbox `- [ ]`. Ist eine **Top-Level-Aufgabe** fertig, wandert sie als Einzeiler nach „Erledigt" (Was + Datum + Kürzel) und verschwindet aus „Noch Offen" — so schrumpft die offene Liste. **Ausnahme:** fertige **Unterpunkte einer noch offenen Aufgabe** bleiben abgehakt (`- [x]`) an Ort und Stelle stehen, weil sie Fortschritt *innerhalb* einer lebenden Aufgabe dokumentieren.
>
> **TDD-Regel (nicht verhandelbar):** Jedes neue Feature/jeder Fix wird test-first gebaut — erst rot sehen, dann grün, dann Mutation Check. Tests sind nach dem Schreiben eingefroren (Details in CLAUDE.md). Tests sind Teil jedes Features, kein separater Punkt.
>
> **Aufwand-Legende:** S = klein (Einzeiler bis ~1h) · M = mittel (Feature/Refactor + Tests) · L = groß/laufend.

---

# Noch Offen

## Teil A — Release-Härtung (Code-Review IST)

### 🟠 Wichtig (vor Release)

— Aktuell **keine offenen Punkte.** (W1/W2/W3, M1/M4, P6/P8, R3/R4 erledigt → siehe [Erledigt](#erledigt).) Nächste offene Härtung steht in 🟡 Mittel (M3).

> Die R-Punkte stammen aus dem **zweiten Review-Durchgang (2026-06-21)** — freies Gesamt-Lesen + lokaler Max-Effort-Recall-Pass.
> **Verworfen — R1 (vermeintlich „Loops loopen nicht"):** Falsch-Positiv. Beruhte auf der Fehlannahme `!IsOneShot ⟹ muss loopen`. `IsOneShot` ist die *Dispatch-Methode* (`PlayOneShot` vs. `source.Play()`), **orthogonal** zu `loop`. Ein non-OneShot spielt korrekt **einmal** als gemanagte, stopp-/fadebare Quelle — kein Bug. `loop` ist ohnehin ein bekannter **Wachstumskandidat** (CLAUDE.md), kein kaputtes Feature. Nicht erneut als Bug aufmachen.

---

### 🟡 Mittel

#### M3 — Leeres PlayMode-Test-Assembly
- [ ] Assembly `Tests/PlayMode/AudioFramework.Tests.PlayMode.asmdef` (nur asmdef + meta, keine Tests).
- **Problem:** Assembly ohne Inhalt — verwirrend, Ballast im Asset-Store-Bundle. Commit-Historie (`4853eae`) erwähnt PlayMode-Tests, die nicht eingecheckt sind.
- **Fix:** Entweder PlayMode-Tests hinzufügen (ideal zusammen mit K1) oder Assembly + Ordner entfernen.
- **Aufwand:** S

---

### ⚪ Niedrig / Politur & Wartbarkeit

#### P2 — `AudioFollowService` dupliziert die StopSlot-Logik inline
- [ ] `AudioFollowService.cs` (~Z. 46–60) wiederholt `AudioStopService.StopSlot` fast wörtlich. Konsolidieren (StopSlot + `ClearFade` + `SetFollowTarget(null)` + Warnung). Wartungsrisiko. Aufwand: S

#### P3 — Mutable struct `AudioObject` im Array + lokale Kopie in `Dispatch`
- [ ] `AudioObject.cs` / `AudioPlaybackService.cs` (~Z. 111). Aktuell korrekt, aber `AudioObject poolObject = PoolArray[i]` als Kopie ist ein Footgun: künftiges `poolObject.<wertfeld> = …` läuft ins Leere. Kommentar oder `ref`-Zugriff. Aufwand: S

#### P4 — OneShot setzt `source.clip` UND ruft `PlayOneShot`
- [ ] `AudioPlaybackService.cs` (~Z. 116 + 145). `PlayOneShot(clip)` braucht `source.clip` nicht — redundant/irreführend. Aufwand: S

#### P5 — Fade auf OneShots ist konzeptionell unsauber
- [ ] Endet der Clip vor Ablauf der Fade-Dauer, bricht der Sound ab statt zu faden. Kein Crash; Erwartung dokumentieren oder Fade auf Loops beschränken. Aufwand: S

#### P7 — Fehlende Tooltips für `IsOneShot`, `CanHandleAudioSource`, `UseWallCheck`
- [ ] `AudioDataObject.cs`. Vor Release alle Inspector-Felder mit vollständigen Tooltips. Aufwand: S

#### R5 — `InitializePool` validiert die Prefab-Komponenten nicht (Review 2026-06-21)
- [ ] `AudioPoolAcquisitionService.cs` (~Z. 37). `GetComponent<AudioSource>()`/`<AudioLowPassFilter>()` ohne Null-Check → ein falsch gebautes Prefab gibt später einen kryptischen NRE in `GetFreeAudioSourcePoolIndex` (`Source.isPlaying`) statt einer klaren Init-Fehlermeldung. Asset-Store-UX (Idiotensicherheit). Aufwand: S

#### R6 — `AudioSystemConfig` ohne `OnValidate`-Guards (Review 2026-06-21)
- [ ] `AudioSystemConfig.cs`. Kein Guard, dass `DefaultCutoffFreqValue > MinCutoffFreqValue` (oder dass Prefab/TransferObject gesetzt sind). Bei Fehlkonfig (Min über Default) floort die Occlusion sofort → alles dumpf ab Frame 1, ohne Inspector-Warnung. Härtung. Aufwand: S

#### R8 — Veraltete XML-Doc in `CalculateCutoffFrequency` (Coroutine) (Review 2026-06-21)
- [ ] `AudioCoroutineWallCheckService.cs` (~Z. 50). `<param name="hitInfo">`/`<param name="originPos">` passen nicht zur Signatur — Copy-Paste-Rest, doc rot in einer ausgelieferten Datei. Trivial. Aufwand: S

---

## Teil B — Geplante Features & Roadmap

> Aus CLAUDE.md zusammengeführt. **Roadmap-Split ist weich, kein hartes Gate:** „nach Launch"-Items können vor 1.0 reingezogen werden. Jedes Feature wird red-first/sauber gebaut (siehe TDD-Regel oben).

### Vor Release (1.0) — fest eingeplant

#### Breite-Features (höchster Verkaufshebel — Strategie in CLAUDE.md)
- [ ] **Mixer/Bus + Ducking** — Laufzeit-Volume (Settings-Menü) + per-Kategorie-Ducking (z. B. Dialog senkt Musik), optional an Unity-AudioMixer-Groups koppelbar. Baut auf dem bestehenden `AudioCategory`/`VolumeDictionary`-System auf. **Höchster Verkaufshebel** (fast jedes Spiel braucht Volume-Menü + Ducking).
  - **Richtungs-Revision (2026-06-28): tool-internes Volume-Resolving (Weg „B") ZUERST; Unity-AudioMixer (Weg „A") als spätere, optionale Schicht obendrauf.** Korrigiert die 2026-06-21-Notiz „A/Hybrid gesetzt": die damalige Wahl fiel gegen ein **Strohmann-„B"** („DSP selbst nachbauen"). Das *echte* B — Per-Source-Gain über `source.volume`, Unity macht weiterhin das DSP — wurde nie gegen A abgewogen. Begründung für B zuerst: bedient das „kein FMOD/Wwise, kein Setup"-Kernversprechen (A zwingt den Nutzer, einen AudioMixer-Graph zu verdrahten → sticht ins eigene Verkaufsargument) und ist **durchgängig** pure-testbar (bei A nur die Entscheidung, nicht die Anwendung). A bleibt jederzeit additiv nachrüstbar (s. „Zwei-Gain-Stufen").
  - **Zwei-Gain-Stufen-Modell (der Schlüssel, warum B keine Türen schließt):** Unitys Signalweg ist `AudioSource(source.volume) → outputAudioMixerGroup → Effekte/Sends → Master → Output`. Zwei getrennte, von Unity verkettete Stufen:
    - **Stufe 1 (vorne, JETZT, gehört B):** `source.volume = basis(kategorie) × fade(slot) × duck(kategorie)`, auf 0..1 geklemmt. **Ein** Besitzer.
    - **Stufe 2 (hinten, SPÄTER, additiv):** Routing/Effekte/Reverb/Sends/Snapshots — alles *downstream* von `source.volume`, hängt nur an `outputAudioMixerGroup` (Reverb-Zonen an `reverbZoneMix`), **nicht** daran, wie `source.volume` berechnet wird.
    - **„Hybrid"-Gedanke bleibt, breiter angewandt:** Duck-*Entscheidung* lebt pure/testbar (wie `AudioFadeMath`); die *Anwendung* ist austauschbar — `source.volume` heute → optional `mixer.SetFloat(dB)` später.
  - **Geklärtes Modell (Gespräch 2026-06-28):**
    - **Volume-Gleichung & Resolver:** pure `VolumeResolver.Resolve(basis, fade, duck) → clamp(basis·fade·duck)`. `basis[kategorie]` = bestehendes `VolumeDictionary` (Settings-Slider). `fade[slot]` = 0..1-Faktor pro Slot (Fade wird von „schreibt `source.volume`" auf „liefert Faktor" umgebaut). `duck[kategorie]` = 0..1, gleitet pro Frame Richtung Ziel (Target + Glide, wie Occlusion).
    - **Ducking ausschließlich pro Kategorie.** Beliebig viele Nutzer-Kategorien.
    - **(a) Tiefe pro (Trigger→Ziel)-Paar** („Duck-Matrix"), je Paar ein eigener Wert (Dialog→Ambience 0.5, Dialog→SFX 0.8), als **sparse explizite Liste** gespeichert (nur konfigurierte Paare). Der Resolver sieht weiterhin nur **einen** `duck[kategorie]`; die Paare sind nur die Eingänge.
    - **(b) Stärkster Duck gewinnt (`min`)** über alle aktiven Trigger einer Zielkategorie — nicht multiplizieren.
    - **(c) Attack/Release global** (ein Wert fürs ganze System), bewusst nicht pro Regel — schützt „easy to learn".
    - **(d) Optionale MonoBehaviour-Komponente auf demselben GO wie `AudioManagerDynamic`** als **passiver Provider** (Config + Laufzeit-Knöpfe), **kein** zweiter `LateUpdate`-Ticker. Der Manager behält den **einen** Tick und den **einen** `source.volume`-Schreiber und fragt die Komponente per Interface (`IDuckRuleProvider`) ab — gleiches Seam-Muster wie `IAudioWallCheckService`/`IAudioListenerProvider`. Komponente fehlt → `duck = 1.0`, Duck-Scan komplett übersprungen → **null Zusatzkosten pro Frame** (Test für „wirklich optional"). Lebenszyklus: Registrierung im `OnEnable`, Abmeldung im `OnDisable`, **kein** `Instance`-Zugriff im `Awake`. `[RequireComponent(typeof(AudioManagerDynamic))]` auf der Duck-Komponente. **Komponente statt SO**, weil die Laufzeit-Tiefen mutabler Per-Szenen-Zustand sind (SO-Asset-Mutation-Footgun).
    - **„Aktiv"-Ableitung:** „Kategorie aktiv" = hat einen spielenden, **nicht-pausierten** Sound; **pro Frame aus dem Pool abgeleitet**, kein +1/-1-Mitzählen (OneShot endet selbst → ableiten, nicht buchführen; gleiche Philosophie wie der self-healing Listener). Setzt `AudioCategory` als unbedingt geschriebenes Feld auf `AudioObject` voraus (Control-Surface).
  - **Leitplanken (nicht überschreiten):**
    - **Gold-Plating-Linie:** JETZT nur die nicht-verbauende **Form** (`{ ziel, tiefe }` pro Paar) + **Live-Read** der Tiefen pro Frame bauen. **NICHT** bauen: Slider-API, Inspector-Matrix, Per-Zellen-Laufzeit-Tooling. „Zur Laufzeit ansprechbar" fällt durch Form + Live-Read gratis ab; das Tooling ist ein späteres, separates Feature.
    - **Easy-to-learn:** Per-Zellen-Feinjustierung bleibt opt-in/advanced, nie Default-UX (sonst sieht der Erstnutzer eine N×N-Slider-Matrix → „easy to learn" weg).
  - **Test-getriebene Schritt-Kette (jeder Schritt rot-zuerst, System bleibt lauffähig):**
    - **Stand 2026-07-02:** Schritte **1 + 2 + 3 + 4 (Flatten-Kern) erledigt.** Schritt 1 — `VolumeResolver.Resolve(basis, fade, duck)` (committet `deab831`). Schritt 2 — `DuckEnvelope.Step(...)` in `Services/DuckEnvelope.cs` (Namespace `AudioFramework.Services.Mixing`) + 8 EditMode-Tests; committet `8466ac4` (die frühere „noch nicht committet"-Notiz war veraltet). Schritt 3 — `DuckTargetPolicy.ResolveDuck(target, activeCategories, pairs)` + pure Input-Struct `DuckPair` (Feld `DuckedVolume`, multiplikative Faktor-Semantik, **keine Inversion**) in `Services/DuckTargetPolicy.cs` + `Services/DuckPair.cs` + 8 EditMode-Tests in `DuckTargetPolicyTests.cs`. Voller Loop **gate-für-gate** durchlaufen (rot → grün → Mutation [Selbst-Duck-Guard entfernt → nur `SelfDuckSkipped` rot, wie vorhergesagt] → grün), jeder Übergang von Patrick bestätigt. Schritt 4 — pure `DuckRuleFlattening.Flatten(rules, results)` (Fill-Stil, nested `DuckRule`→flache `DuckPair`, kein Filtern) + serialisierte Typen `DuckRule`/`DuckTarget` (`Config/`, Namespace `AudioFramework.Configuration`, mit `[Range]`/`[Tooltip]`-Politur) + 8 EditMode-Tests (`DuckRuleFlatteningTests.cs`, Mutation = `results.Clear()` entfernt → nur `ResultsList_IsClearedBeforeFill` rot). **Stand 2026-07-02 (Session-Ende): Kette 1–6 komplett, Feature läuft.** Schritt 5 — `AudioCategory Category` auf `AudioObject` + `SetCategory`-Einzeiler + unbedingter Write in `Dispatch`; **TDD bewusst aufgelockert** (logikfreies Plumbing, kein Branch/keine Berechnung → kein ehrlicher red-first-Test, Patricks Go). Schritt 6 — kompletter Runtime-Glue: `AudioObject.FadeFactor` + `AudioDuckService` (**einziger `source.volume`-Besitzer**: `basis·fade·duck` live pro Frame, GC-frei über wiederverwendete Buffer) + passive `AudioDuckComponent` (`IDuckRuleProvider`, `[RequireComponent(typeof(AudioManagerDynamic))]`, OnEnable/OnDisable-Registrierung, **kein eigener Tick**) + reservierter `CategoryMixerRoute`-Stufe-2-Seam + globale Attack/Release als **Rate** (Faktor-Einheiten/s, 1:1 auf `DuckEnvelope`). **Fade→Faktor-Besitzerwechsel durchgeführt** (Loop-Regel #6, Patricks ausdrückliches Go): `PooledFadeTarget.Volume` schreibt den Per-Slot-Faktor statt `source.volume`, totes `out targetVolume`-Plumbing entfernt; **eingefrorene Fade-Tests unberührt/grün** (testen die Rampe über `FakeFadeTarget`, agnostisch wozu `Volume` zeigt). **Schritt 6 hat keine neue pure Logik** — alles in 1–4 getestet; Glue direkt gebaut, PlayMode-Smoke offen (s. Folgeaufgaben). **Noch nicht committet** (Patrick committet selbst, inkl. Schritt 5).
    1. Pure `VolumeResolver` (`basis·fade·duck`, clamp). Kleinster erster Stein. ✅
    2. Pure `DuckEnvelope.Step` (Glide; Attack beim Tiefer-Ducken / Release beim Zurück) — Zwilling von `OcclusionSmoothing.Step`. ✅
    3. Pure `DuckTargetPolicy` (aktive Kategorien + Paar-Faktoren → `duck[kategorie]`; `min`-Stacking; Kategorie duckt sich nie selbst; **keine Kaskade** — liest nur Active-Set-Membership, ein geduckter Trigger triggert mit voller Stärke). ✅
    4. Config-Daten: `DuckRule { trigger, ziele: [{ kategorie, tiefe }] }` + globale Attack/Release; **plus** reservierter (leerer) `Kategorie → AudioMixerGroup`-Seam für Stufe 2. Leichte `OnValidate`-Guards. ✅ *(Flatten-Kern + serialisierte Typen + D3-Politur erledigt; **globale Attack/Release + Mixer-Seam nach Schritt 6 verschoben**, da Komponenten-Felder.)* **UI-Entscheidung (2026-07-02): Faktor-Semantik bleibt, keine Inversion in der puren Schicht — die Verständlichkeit kommt allein über den Inspector-Tooltip, wörtlich „duckt die Ziel-Kategorien AUF X % ihrer Lautstärke". Der User tippt direkt den Faktor (0.9 = auf 90 %), der Text erklärt ihn.**
    5. `AudioCategory` auf `AudioObject` (unbedingt bei jedem Dispatch geschrieben). ✅ *(Feld `Category` + `SetCategory` + unbedingter `Dispatch`-Write; logikfreies Plumbing, TDD bewusst aufgelockert nach Patricks Go — echte Coverage fällt beim PlayMode-Smoke.)*
    6. Runtime-Glue `AudioDuckService` (im Manager-Tick): aktive Kategorien ableiten → Ziele (3) → gliden (2) → `source.volume` auflösen (1). PlayMode-Smoke. Hier fällt der **Live-Settings-Slider** gratis ab. **Zusätzlich hierher gezogen (2026-07-02, aus Schritt 4):** die passive MonoBehaviour-Komponente + `IDuckRuleProvider` + Lifecycle (`OnEnable`/`OnDisable`), die Global-Attack/Release-Felder und der reservierte (leere) `Kategorie → AudioMixerGroup`-Seam (Stufe 2). ✅ *(alles gebaut: `AudioDuckService` als einziger `source.volume`-Besitzer, `AudioDuckComponent` + `IDuckRuleProvider` + Lifecycle, `CategoryMixerRoute`-Seam, `AudioObject.FadeFactor`. Attack/Release als Rate. PlayMode-Smoke bewusst aufgeschoben — s. Folgeaufgaben.)*
  - **Offene Folgeaufgaben (Kette 1–6 gebaut, Feature läuft — Rest bis release-fertig):**
    - [ ] **PlayMode-Smoke für die Duck-Glue** — Fade rampt `source.volume` über Frames via Faktor; Ducking senkt eine Zielkategorie, während ihr Trigger spielt, und kehrt bei dessen Ende zurück; eine Live-Änderung im `VolumeDictionary` schlägt sofort auf `source.volume` durch (Slider). Coverage für die gesamte Schritt-6-Glue (keine pure Logik → EditMode-untestbar). Bündelt mit M3/K1 + den anderen PlayMode-Smokes.
    - [ ] **UX-Entscheidung Attack/Release: Rate vs. Zeit** — aktuell als **Rate** (Faktor-Einheiten/s) exponiert (1:1 auf `DuckEnvelope`, null Umrechnungslogik). Designer denken oft in **Zeit** (ms bis volle Dämpfung). Falls zeitbasiert gewünscht: bewusste kleine Umrechnung *mit eigenem Test*. Patricks Call.
    - [ ] **`OnValidate`-Guards auf `AudioDuckComponent`** — Attack/Release ≥ 0, evtl. Doppel-Trigger-Warnung. Asset-Store-Politur (war als „leichte Guards" in Schritt 4 vorgesehen).
    - [ ] **Doku-Sync (nach Patricks Gegenlesen):** User-Doku DE/EN um einen Ducking-Abschnitt (Komponente drauf, Regeln, Slider); **CLAUDE.md**: der neue `source.volume`-**Ein-Besitzer** + Fade-als-Faktor + Zwei-Gain-Stufe-1-Fakt als IST-Zustand.
  - **Scharfer Edge (Loop-Regel #6) — ERLEDIGT (Schritt 6, 2026-07-02):** Der Fade→Faktor-Besitzerwechsel (Fade schreibt nicht mehr direkt `source.volume`, sondern den Per-Slot-Faktor; `AudioDuckService` ist einziger Besitzer) wurde nach **Patricks ausdrücklichem Go** durchgeführt. Die eingefrorenen Fade-Tests wurden **nicht** obsolet — sie testen die Rampen-Mathematik über `FakeFadeTarget`, unabhängig davon, worauf `Volume` physisch zeigt → **kein Test-Rewrite nötig, kein Test angefasst.** Ursprüngliche Auflage (bei Obsoletwerden: Eintrag + Go, kein stiller Rewrite) blieb damit unberührt.
  - **Geklärte Fragen (entschieden):**
    - **(geklärt 2026-06-28) Kaskaden-Semantik: KEINE Kaskade — ein geduckter Sound bleibt ein voller Trigger.** Triggert eine Kategorie, die selbst gerade geduckt wird, eigene Regeln, geschieht das mit *voller* Triggerstärke (Beispiel: A duckt B; B duckt weiterhin C mit voller Stärke, NICHT mit seinem schon reduzierten Volume). Keine Fixpunkt-Iteration. Hält den Resolver einstufig + reihenfolge-unabhängig, verhindert Oszillation. *Von Patrick bestätigt.*
    - **(geklärt 2026-06-28) Stufe-2-Scope: alle Stufe-2-Features kommen später — JETZT so bauen, dass sie additiv & leicht nachrüstbar bleiben.** Patrick will später das volle Stufe-2-Paket (Reverb-Zonen via `reverbZoneMix`, Sends, Snapshots, `outputAudioMixerGroup`-Routing). Konsequenz für B jetzt: den reservierten (leeren) `Kategorie → AudioMixerGroup`-Seam (Schritt 4) wirklich anlegen und die Zwei-Gain-Stufen-Trennung sauber halten (Stufe 1 = `source.volume`-Besitz, Stufe 2 strikt downstream), damit kein Stufe-2-Feature später eine Tür finden muss, die B zugemauert hat. Nichts davon jetzt implementieren — nur die Form nicht verbauen. *Von Patrick bestätigt.*
- [ ] **Random Pitch/Volume-Variation** — pro ADO optionale Streuung gegen den „Maschinengewehr-Effekt" bei wiederholten One-Shots (Footsteps, Hülsen). Billiger, sofortiger Qualitätsgewinn; greift mit den layer-reaktiven Sounds ineinander.
  - **Wettbewerbs-Kontext (2026-07-04):** Sonity (€91) hat Pitch/Volume-Randomisierung **und** Layering pro Sound; du hast bereits Clip-Randomisierung (`CurrentClips[Random.Range(...)]` in `AudioPlaybackService`), aber keinen Jitter. Von den drei Zielgruppen-Lücken (siehe No-Code-Trigger, Polyphonie unten) ist das die **billigste und sofort hörbarste** — höchste Priorität der drei.
  - **Teststrategie (test-first):** Die Streuung ist reine Mathe → pure `PitchVolumeVariation.Apply(basePitch, baseVolume, pitchRange, volumeRange, roll)` mit **injiziertem** Zufallswert `roll ∈ [0,1)` (Determinismus-Seam wie `AudioFadeMath`), EditMode-getestet (Randfälle: `range = 0` ⟹ Basiswert unverändert; `roll = 0`/`0.5`/`≈1` ⟹ untere/mittlere/obere Grenze; Volume auf `[0,1]` geklemmt). Der Aufruf von `UnityEngine.Random` bleibt an der Unity-Grenze, die *Berechnung* ist pure. Neue ADO-Felder (`PitchVariation`, `VolumeVariation`, `[Range]`-geguardet) sind gespiegelte Control-Surface-Werte → in `Dispatch` unbedingt schreiben (setzt voraus, dass `pitch` als Control-Surface-Feld existiert — Wachstumskandidat aus dem ADO-Härten-Task).
  - **Aufwand:** S–M
- [ ] **No-Code-Trigger-Komponente** (Wettbewerbs-Lücke, 2026-07-04) — eine `MonoBehaviour`-Komponente (Arbeitstitel `AudioTrigger`), die man auf ein GameObject legt und im Inspector mit einem ADO + einem Auslöse-Event verdrahtet (`OnEnable`/`OnDisable`, `OnTriggerEnter`/`OnCollisionEnter`, oder ein `UnityEvent`-Hook) — Sound **ohne eine Zeile Code**. Spiegelt Sonitys `SoundTrigger`/`SoundPhysics`.
  - **Warum (größte Zielgruppen-Lücke):** Das Tool ist heute code-first (`AudioManagerDynamic.PlaySpatial(...)`). Deine Zielgruppe sind aber genau die Indies/Nicht-Programmierer, die Drag-&-Drop wollen — die Lücke sticht paradox ins eigene Verkaufsargument. Sonity **und** MasterAudio liefern No-Code-Trigger; das ist der stärkste Adoptions-Hebel für Einsteiger.
  - **Sauberes Design (kein AudioSource-Besitz):** Die Komponente ist ein **passiver Dispatcher** — sie ruft nur die öffentliche API und hält den zurückgegebenen `AudioHandle` (für Stop-on-Exit bei Loops). **Nie** eigene `AudioSource` verwalten (würde das Pooling-Modell umgehen). Lebenszyklus wie `AudioDuckComponent`: Registrierung/Feuern in `OnEnable`/`OnTriggerEnter`, Aufräumen in `OnDisable`; **kein** `Instance`-Zugriff im `Awake`. Optional `RespectsGlobalPause`/Handle-Stop sauber durchreichen.
  - **Teststrategie (Seam-first, ehrliche Tests):** Die dünne Unity-Glue (MonoBehaviour-Callbacks) ist PlayMode-Smoke. Die **Entscheidungslogik** in einen pure Seam ziehen und EditMode-testen — z. B. `TriggerPolicy` (Debounce/Cooldown: darf jetzt erneut feuern? · One-Shot vs. Loop: Handle behalten oder wegwerfen? · Re-Entry-Guard bei überlappenden Collidern). Muster wie `IDuckRuleProvider`/`WallCheckContinuation`: Komponente hält den Zustand, die *Entscheidung* lebt pure.
  - **Aufwand:** M
- [ ] **Polyphonie-/Voice-Limiting-Gruppen** (Wettbewerbs-Lücke, 2026-07-04) — Obergrenze gleichzeitiger Stimmen **pro Kategorie/Gruppe** mit Steal-Policy (leiseste bzw. älteste Stimme weicht), wenn das Limit erreicht ist. Sonitys `SoundPolyGroups` + Voice-Stealing.
  - **Warum:** Löst genau den **einzigen echten Verlust des Einzel-Pools**, der im „Multi-Pool VERWORFEN"-Abschnitt notiert ist (Kapazitäts-Partitionierung: spammy Footsteps/Impacts hungern einen wichtigen Occlusion-Sound aus). Sonity adressiert das direkt; für jede anspruchsvollere Szene real. Baut auf dem bereits geschriebenen `AudioObject.Category`-Feld (Ducking-Schritt 5) auf — der Gruppenschlüssel existiert schon.
  - **Teststrategie (test-first):** Die Steal-*Entscheidung* ist pure → `PolyGroupPolicy.SelectVoiceToEvict(activeSlots, cap, newRequest) → slotIndex | reject` (rohe Werte: Volumen, Startzeit, Cap — Unity-frei, EditMode-testbar wie `DuckTargetPolicy`). Randfälle: unter Cap ⟹ kein Evict/kein Reject; genau am Cap ⟹ leiseste/älteste (Policy-Wahl vorher spezifizieren!) weicht; gleicher Rang ⟹ deterministische Tie-Break-Regel. Die Pool-Integration (den gewählten Slot wirklich via `AudioStopService.StopSlot` beenden) ist Unity-Glue. Config: Cap pro Kategorie, analog zu `VolumeDictionary`/`DuckRule`.
  - **Offene Spec-Frage vor Bau:** Steal-Kriterium (leiseste vs. älteste vs. niedrigste `priority`) + ob ein neuer Request bei vollem Cap **abgewiesen** oder eine bestehende Stimme **verdrängt** wird. Mit Patrick klären, bevor der erste rote Test steht.
  - **Aufwand:** M
- [ ] **Adaptives/interaktives Musik-Layer** — mehrere Musik-Stems, je nach Spielzustand ein-/ausgeblendet (nutzt die Fade-Familie). Größter Abstand zu FMOD/MasterAudio, größter „Wow"-Effekt — und der größte Brocken.

#### Release-Hygiene
- [ ] **Tooltips** — alle Inspector-Felder vollständig (insb. `IsOneShot`, `CanHandleAudioSource`, `UseWallCheck`). (= P7 oben.)
- [ ] **`IGetPoolIndex` entfernen** — toter Platzhalter (Lightweight-Pool über per-Sound-Flags gelöst, siehe „Reißbrett" unten). War in der „nicht ohne Rücksprache"-Liste — diese Notiz IST der Rücksprache-Trigger.
- [ ] **Null-Einträge in `CurrentClips` validieren** (= R2, Schwere geschärft 2026-06-21) — `Random.Range` kann einen `null`-Eintrag wählen. Die Validierung in `Dispatch` (~Z. 95) guardet nur `CurrentClips == null || Length == 0`, **nicht** einzelne null-Elemente. Bei OneShot ist `SetSlotBusy(poolIndex, currentClip.length)` (~Z. 135) dann eine **NullReferenceException** — schärfer als das ursprüngliche „stiller Sound". Fix: null-Eintrag beim Pick skippen/warnen, oder ADO-Inspector-Validierung.
- [ ] **Ausgeliefertes `AudioSystemConfig`-Asset fehlt im Repo** (entdeckt 2026-06-21 beim Doku-Review) — die User-Doku verweist mehrfach auf „das **mitgelieferte** `AudioSystemConfig`-Asset, bereits vollständig vorkonfiguriert" (Step 2 + Step 5). Im Repo liegt aber nur `Assets/ScriptableObjects/AudioConfigs/ConfigForTests.asset` (Typ `AudioSystemConfigSO_Test`), KEIN produktives `AudioSystemConfig.asset`. Vor Release klären: ein vorkonfiguriertes Produktions-Asset anlegen + ins Package legen (sonst stimmt die Doku nicht / Erstnutzer hat keine Config). Gesondert behandeln, nicht Teil des Doku-Updates.
- [ ] **PDF-Dokumentation** — `.md`-Dateien existieren (DE/EN), Konvertierung zu PDF offen.
- [ ] **Ordnerstruktur für Asset Store** — noch nicht definiert.
- [ ] **IP-Schutz: Core als vorkompilierte DLL ausliefern, nur Nötigstes als Source** (Plan 2026-06-21) — Ziel: Käufer sollen das Tool nicht einfach 1:1 als Source kopieren und „öffentlich" reposten/weiterverkaufen können. **Mechanismus:** `AudioFramework.asmdef` vorkompilieren und als **DLL** ins Package legen statt der `.cs`-Dateien; nur die Typen, die der Nutzer *editieren muss*, bleiben als sichtbare Source — **minimal nur das `AudioCategory`-Enum** (alles andere konfiguriert er über Inspector/ScriptableObjects, nicht im Code).
  - **Architektur-Task:** `AudioCategory` (und evtl. künftige bewusste Erweiterungspunkte) in eine **eigene, als Source ausgelieferte asmdef** ziehen, die die Core-DLL referenziert. Vorab verifizieren, dass der Enum-Edit-Flow weiter funktioniert: Nutzer ändert das Enum → Unity rekompiliert die Source-asmdef → die Core-DLL bindet per Assembly-Referenz dagegen (Enum-Erweiterung bricht den Vertrag nicht). Test-asmdef referenziert Internals → bei DLL-Auslieferung `InternalsVisibleTo` klären oder Tests gar nicht erst mitliefern.
  - **Ehrlicher Vorbehalt (nicht überschätzen):** Eine .NET-DLL ist **trivial dekompilierbar** (ILSpy/dnSpy/dotPeek). DLL stoppt Casual-Copy-Paste und „kompletten Source public posten", aber **nicht** den Entschlossenen. Obfuscation hebt die Hürde, macht es nie unmöglich (abnehmender Ertrag). Die eigentliche *rechtliche* Schutzschicht ist ohnehin die Asset-Store-EULA — die DLL ist die technische Hürde obendrauf, kein Tresor.
  - **Strategische Abwägung (vor Umsetzung bewusst entscheiden):** „Sauberer, lesbarer, erweiterbarer Code" ist aktuell Teil deines Verkaufsarguments — eine DLL **versteckt genau diese Stärke**. Manche kleinen Studios *wollen* lesbare Source zum Verstehen/Erweitern. Kompromiss prüfen: Core als DLL, aber Trust-Signal anders transportieren (öffentliche API sauber dokumentiert + „ships with N passing tests" bewerben, statt den Source-Beweis mitzuliefern). Entscheidung: Schutz vs. Lesbarkeit — Patricks Markt-Call.
- [ ] **Demo-Szene für 60-Sek-Verkaufsvideo** (Plan 2026-06-21; **erst bauen, wenn die Breite-Features drin sind** — Mixer/Bus + Ducking, Pitch/Volume-Variation, adaptive Musik-Layer). Eine Szene, in der jedes Kern-Feature in ~60 s vorführbar ist — das ist das **Verkaufsasset Nr. 1** (Käufer kaufen nach Demo + Breite, nicht nach Test-Coverage). Muss zeigen: (a) **Occlusion live** — hinter eine Wand laufen, Sound wird dumpf, weiches Zurückgleiten beim Heraustreten; (b) **Ducking live** — Musik läuft, Dialog setzt ein → Musik duckt, Dialog endet → Musik kommt zurück; (c) **Crossfade** — Ambient-Zonen-Wechsel hörbar überblenden; (d) **Pooling-Stress** — ~30 Sounds gleichzeitig ohne GC-Spike, mit sichtbarem Profiler; (e) **Volume-Slider** — Live-Kategorie-Lautstärke. Gemeinsam mit Patrick strukturieren.
- [ ] **PlayMode-Smoke-Tests für die Fade-Glue** — fadet `source.volume` wirklich über Frames, gibt FadeOut den Slot frei, friert echtes `PauseAll` einen Fade ein. **Plus M1-Fall:** bei `Time.timeScale = 0` läuft ein laufender Fade (und der Occlusion-Glide) weiter — `source.volume` ändert sich über Frames trotz `timeScale = 0` (beweist `unscaledDeltaTime`). Locked die aktuell nur manuell verifizierte Verdrahtung. Gutes Bündel mit M3 + K1-PlayMode-Test.
- [ ] **PlayMode-Smoke-Test für `SceneAudioListenerProvider`** (aus W3) — in kontrollierter Szene das Self-Heal beweisen: (a) `TryGetPosition` liest die *aktuelle* Listener-Position (nicht die Start-Position), (b) nach Disable des alten + Enable eines neuen Listeners liefert er den neuen, (c) nach Zerstörung des Listeners `false`. Bewusst PlayMode statt EditMode, weil `FindObjectsByType` sonst den AudioListener der offenen Editor-Szene fände. Bündelt mit M3 + K1.
- [ ] **PlayMode-Smoke-Test für die timeScale-Familie** (aus M4 + R4) — bei `Time.timeScale = 0`: (a) ein OneShot-Slot wird nach Clip-Ende korrekt **freigegeben** (hängt nicht mehr — beweist `Time.unscaledTime` im Busy-Fenster), (b) der WallCheck eines weiterspielenden `RespectsGlobalPause=false`-Sounds **tickt weiter** und aktualisiert `TargetCutoff` (beweist `UnscaledDeltaTime`/`WaitForSecondsRealtime`). Plus Gegenprobe: ein OneShot-Slot überlebt eine echte `PauseAll` und wird **nicht zu früh** freigegeben. Bündelt mit M3 + K1 + den Fade-Smoke-Tests.

### Weitere geplante Features (Priorität offen — können vor 1.0 rein)

- [ ] **ADO Control Surface härten + erweitern** (Plan 2026-06-21) — die „ADO-ist-Control-Surface"-Invariante (jedes gespiegelte Feld MUSS bei JEDEM Dispatch unbedingt geschrieben werden, sonst trägt ein wiederverwendeter Slot den Vorgänger-Wert — so entstand der `spatialBlend`-Bug) lebt heute als **Disziplin** im flachen `Dispatch`-Block. Bei wachsender Feldzahl wird das vergessbar. **Zweistufig, Seam ZUERST (mit Patrick entschieden):**
  - **Stufe 1 — Invariante als Struktur statt Disziplin (Vorarbeit):** einen `ApplyControlSurface(source, ado)`-Seam ziehen, der ALLE reinen Spiegel-Felder unbedingt schreibt → eine offensichtliche Stelle für neue Felder (Footgun weg) + **EditMode-Unit-Test-Ziel** („gegeben ein ADO, landet *jedes* gespiegelte Feld auf der Source", via Fake-Source-Seam). Saubere Trennung: reine Spiegel in `ApplyControlSurface`; die **berechneten** Felder bleiben Entscheidungslogik in `Dispatch` (`spatialBlend` ⟵ isSpatial, `filter.enabled`/cutoff ⟵ UseWallCheck via `LowPassDispatchPolicy`, volume ⟵ startSilent/`ResolveVolume`). Test-first wie immer.
  - **Stufe 2 — Felder nachschieben** (nach dem Seam, je test-first), bereits geplant: `pitch`, `loop`, `priority`, `minDistance`/`maxDistance`, `rolloffMode`, `spread`. **Neu ergänzt (2026-06-21):**
    - **`dopplerLevel`** ⭐ — natürlicher Partner von `FollowEmitter`; Doppler ist genau der Effekt für bewegte Emitter (beworbener Use-Case „Motor-Loop eines vorbeifahrenden Autos"). Gehört mit Follow zusammen.
    - **`panStereo`** — L/R-Pan für 2D-Sounds (`PlayNonSpatial`). Billig, sinnvoll fürs 2D-Spektrum.
    - **`reverbZoneMix`** — Speisung in Reverb-Zonen; nur falls Reverb-Zonen in Scope kommen (Hand in Hand mit dem Mixer-Thema), sonst zurückstellen.
  - **Bewusst NICHT aufnehmen (Begründung, damit die Surface nicht aufquillt):** `spatialize`/`spatializePostEffects` (HRTF-Plugin) — koppelt an Steam Audio/Oculus, widerspricht dem „lightweight occlusion, kein voller Spatializer"-Verkaufspunkt. `ignoreListenerPause`/`ignoreListenerVolume` — überschneidet sich mit dem eigenen Pause-Modell (`RespectsGlobalPause`), zwei Pause-Konzepte = Verwirrung. Custom-Rolloff-`AnimationCurve` — Power-Feature, aber schwerer (Curve-Serialisierung) → „später, wenn nachgefragt". `mute` als serialisierter Default — sinnlos (warum einen stummen Sound ausliefern).
  - **Anmerkung zu `loop` vs. `IsOneShot`:** orthogonal — `loop` greift nur auf dem `source.Play()`-Pfad (nicht bei `PlayOneShot`). Tooltip entsprechend setzen.
  - **Doku-Sync (separat, nach Patricks Gegenlesen):** die Wachstumskandidaten-Liste in `CLAUDE.md` (Abschnitt „ADO ist die Control Surface") um `dopplerLevel`/`panStereo`/`reverbZoneMix` ergänzen, sobald gebaut.

- [ ] **Layer-basierte reaktive Geräusche** — Sounds je nach getroffenem Layer unterschiedlich (Footstep auf Holz vs. Metall vs. Boden). Mapping Layer/Material → AudioClip(-Gruppe), analog zum Layer→Cutoff-Dictionary.
- [ ] **Occlusion-Spawn-Snap** (Mini) — ein Sound, der DIREKT hinter einer Wand startet, gleitet aktuell ~0.3 s hell→dumpf. Optional „beim allerersten WallCheck-Tick snappen, danach glätten" via `hasTarget`-Flag pro Slot. Nur falls es stört.
- [ ] **Occlusion: Doppel-Dämpfung an überlappenden Wand-Ecken** (diskutiert 2026-07-02) — überlappen sich zwei Wand-Collider an einer Raumecke, zählt ein einzelner Raycast beide als getrennte Wände → der Sound wird **zweifach** gedämpft, obwohl er nur knapp hinter der Kante liegt. **Wurzel:** das Modell zählt Oberflächen-Durchtritte als *Proxy* für occludierende Materialmenge; an Ecken/Überlappungen entkoppelt sich dieser Proxy. Effekt ist transient (schmales Winkelfenster), durch die Occlusion-Glättung teilweise maskiert und durch `MinCutoffFreqValue` nach unten begrenzt — aber überlappende Collider (gängiges modulares Authoring-Muster: Prefabs überlappen, um Nähte/Lichtlecks zu vermeiden) verbreitern das Fenster. **Bereits erledigt:** als Known Limitation dokumentiert (User-Doku DE/EN, dedizierte Sektion „Bekannte Einschränkungen" / „Known Limitations" am Dokumentende; Einzeiler-Verweis im Wall-Check-Kapitel) + Authoring-Guidance (Collider nicht überlappen; ein Collider pro Wandstück / einzelner Eck-Collider). **NICHT spekulativ bauen** — erst auf echtes Nutzer-Feedback hin, dass es hörbar stört. *Falls* dann gebaut, ist die saubere proportionale Lösung eine **pure Hit-Dedup-Funktion neben `WallOcclusionMath`**: Treffer, die entlang des Strahls näher beieinander liegen als eine konfigurierbare Mindest-Wanddicke, vor dem Dämpfen zu einem zusammenfassen (Hit-Distanzen sortieren → mergen; Unity-frei, EditMode-testbar, echter Modell-Seam). **Ehrlicher Vorbehalt:** kostet einen neuen Tunable (Mindest-Wandabstand) + ein echtes **False-Merge-Risiko** — zwei *wirklich* getrennte, aber dicht stehende Dünnwände (Doppelwand-Konstruktion, schmaler Spalt) würden fälschlich verschmolzen; man tauscht einen seltenen Artefakt gegen einen anderen. **Verworfen:** Materialdicke echt messen (Eintritt+Austritt pro Collider, Dämpfung ∝ Pfadlänge im Material) wäre physikalisch korrekt, sprengt aber die „ein billiger Raycast"-Einfachheit, die das Verkaufsargument *ist*. Aufwand: M.
- [ ] **Occlusion: Multi-Ray „dicker" Wall-Check** (Wachstumskandidat, diskutiert 2026-07-04; **ganz sicher NICHT vor Release** — Patricks ausdrückliche Ansage) — der heutige Wall-Check castet **einen** mathematisch unendlich dünnen Raycast pro Sound. Fällt die Sichtlinie durch eine schmale Öffnung (Schießscharte, kleines Loch), meldet der Strahl „keine Wand" und der Sound bleibt ungedämpft (als **Known Limitation** dokumentiert, DE/EN). **Abhilfe = dem Strahl „Dicke" geben.** Zwei Wege geprüft: **`SphereCast`** verworfen — er ignoriert Collider, in denen der Sweep startet, und liefert nur die erste blockierende Fläche → bricht das „jede Wand einzeln zählen & dämpfen"-Modell (`WallOcclusionMath.ApplyWall` pro Treffer). **Multi-Ray** (N Strahlen in kleinem Muster um die Richtung) ist der saubere Weg: jeder Strahl durchläuft die bestehende Schleife, danach aggregieren. Kosten skalieren **linear N×**, aber (a) opt-in, (b) das `TimeIntervalBetweenPositionChecks`-Intervall deckelt die Frequenz ohnehin, (c) N klein (3–5). **Netter Nebeneffekt:** N-Strahlen **mitteln** (statt „irgendeiner offen ⟹ offen") liefert *echte Teil-Okklusion* durch einen Schlitz — halbe Strahlen blockiert → halb gedämpft. **Sauberer Home:** pure, EditMode-testbare `WallRaySampler`-Funktion (Richtung + Perpendikular-Basis + Radius → N Sample-Richtungen), Modell-Seam wie `WallOcclusionMath`. **Einbettung (mit Patrick entschieden): globaler Schalter in `AudioSystemConfig`** (`WallCheckQuality: Single | Thick` + Ray-Count/Radius), **NICHT** als ADO-Unteroption — der Bedarf kommt aus der Level-Geometrie, nicht aus dem Sound; hält die Control Surface schlank. Per-ADO-Override nur additiv später, falls echter Fall. **Offene Spec-Frage vor Bau:** Aggregations-Semantik (Mittel vs. min/max). **Ehrlicher Vorbehalt:** auf Konsole/Mobile multiplizieren sich N× Raycasts **und** die schon dokumentierte Per-Source-DSP-Filterlast → muss **default AUS** sein. Aufwand: M.
- [ ] **README-Einzeiler „IL2CPP/AOT-safe" — erst NACH einem echten IL2CPP-Build eintragen** (diskutiert 2026-07-04) — Code-Scan (2026-07-04) ist sauber: kein `Reflection.Emit`/`dynamic`/`Activator.CreateInstance`/`Marshal`/P-Invoke, `allowUnsafeCode: false`, **keine Native-Plugins**, Serialisierung nur über Unitys eigene; UniTask ist selbst AOT-tauglich + Coroutine-Fallback vorhanden. Der Claim ist damit **code-theoretisch** ehrlich — wird aber erst zu „verifiziert", wenn Patrick **einmal einen IL2CPP-Player** gebaut hat (z. B. Windows- oder Android-IL2CPP-Build, billig, kein Devkit nötig). **Bis dahin NICHT ins README.** Exakter, freigegebener Wortlaut (nur dieser Satz, KEINE Konsolen-Namen — „läuft auf PS/Xbox/Switch" wäre ungetesteter Plattform-Support und unehrlich):
  > *„Pure C#, no native plugins, no runtime reflection or codegen — builds cleanly on both Mono and IL2CPP/AOT targets."*
  Doku-Regel beachten: README EN + `README.de.md` 1:1 spiegeln. Aufwand: S (nach dem Build).
- [ ] **CTS-Reuse im WallCheck** — pro `Play()` mit WallCheck wird eine `CancellationTokenSource` alloziert (`CreateLinkedTokenSource`) → „Zero GC" stimmt noch nicht ganz. Reuse / `TryReset`.
- [ ] **Multiplayer & VOIP** — siehe Reißbrett unten.
- [ ] **VR-Optimierung via RaycastCommand** — Wall-Checks feuern `Physics.Raycast` einzeln auf dem Main Thread. Für mobiles VR (Quest) bei vielen Quellen kritisch. Umstieg auf gebatchte, jobified `RaycastCommand` (Burst/Job System). Architektur ist sonst schon VR-tauglich (kein GC, Pooling, UniTask, ein AudioListener). Konkreter nächster Schritt, sobald VR Zielgruppe wird.

### Größere Vorhaben / Reißbrett

#### Multiplayer & VOIP (zwei getrennte Teile)
1. **Multiplayer (networked sounds):** Tool ist client-seitig — Networking entscheidet WANN/WO, das Spiel ruft `PlaySpatial(ado, networkedTransform)`. Großteils „nichts nimmt Single-Player an"-Pass + dokumentierte Patterns. Ein lokaler AudioListener pro Client ist ok. Viele Remote-Emitter → mehr Spatial-Sounds + Wall-Checks → koppelt an VR/RaycastCommand.
2. **VOIP (der härtere, interessantere Teil):** Großes Verkaufsargument = **Proximity-Voice mit Wall-Occlusion** (Stimme hinter Wand wird gedämpft). Snag: VOIP ist ein **Live-PCM-Stream**, kein Clip aus `ado.CurrentClips`. Braucht einen Pfad, um Occlusion/Follow/Volume/Pause über eine AudioSource laufen zu lassen, die das Tool NICHT aus einem ADO-Clip erzeugt hat → entweder (a) „bring-your-own-AudioSource"-Registrierung in einen Slot, oder (b) Streaming-Clip-ADO-Variante. Bewusst entscheiden. Cross-Cutting: VOIP meist `RespectsGlobalPause=false`, `FollowEmitter` auf Remote-Avatar.

#### Multi-Pool — als eigenständige Architektur VERWORFEN (Referenz)
Die Rationale (lightweight vs. occlusion; UI/2D) ist via per-Sound-Flags am Einzel-Pool gelöst: `UseWallCheck` (Loop startet nur bei true → 0 Raycast sonst), `PlayNonSpatial` (spatialBlend 0), `filter.enabled=false` für nicht-occludierte. Pause-Scope (war DER Blocker) ist via `RespectsGlobalPause`/`IsPaused` gelöst. **Einziger echter Verlust eines Einzel-Pools:** Kapazitäts-Partitionierung (spammy Footsteps können einen wichtigen Occlusion-Sound aushungern). Für ~50 kurzlebige Slots klein/billig zu mitigieren (großzügig dimensionieren; optional Priority/Eviction später). Erst bauen, wenn Patrick heavy simultanes Lightweight-Spam hat — dann sauber via `IAudioPool` + Shared Base, nicht angeklebte `if`s.

### Gesonderte Aufgaben (nach den ersten Features)

- [ ] **Kontext-Refactor: Main- vs. Projektdatei trennen → erweitern → Fragebogen blockweise** — Claudes Kontext entmischen. Schritte in Reihenfolge: **(1)** Aktuelle `CLAUDE.md` dekomponieren in *allgemeingültigen Kanon (gilt für JEDES von Patricks Projekten)* → wandert in eine **user-level Main-Datei `~/.claude/CLAUDE.md`** (lädt Claude Code automatisch in jedes Projekt), und *projektspezifisches AudioTool-Wissen* → bleibt in der projekt-lokalen `CLAUDE.md`. **(2)** Beide Dateien um relevante Kontexte erweitern. Projekt-*Typ* (Asset-Store-Plugin vs. gekapseltes Game-Feature, inkl. „Idiotensicherheit"-Dosierung) NICHT als drittes File, sondern als Abschnitt in der Projektdatei — Fragmentierung/Drift vermeiden. **(3)** Erst danach den **Fragebogen** erstellen und **blockweise/iterativ** durchgehen (z. B. SOLID/Clean Code → Design Patterns → Unity-Idiome → API-/Asset-Store-Härtung), um den Kontext zu schärfen. Fragen sollen Trade-off-Entscheidungen + Grenzen hervorlocken, nicht Lehrbuch-Bekenntnisse. **Patrick trifft die Entscheidungen, Claude entscheidet nicht selbst.** Universelle Konstanten (gelten für alle): Testbarkeit, Wartbarkeit, Erweiterbarkeit, Skalierbarkeit; Unit-Tests immer & überall. Überschneidet sich mit „Coding Guidelines gemeinsam erarbeiten" (ggf. zusammenführen). Aufwand: L (laufend, kollaborativ).

- [ ] **Coding Guidelines gemeinsam erarbeiten** — über die nächsten Sessions verteilt einen festen Satz Code-Konventionen/Stil-Richtlinien mit Patrick ausarbeiten (Naming, Struktur, Kommentare, Test-Konventionen, Service-/Seam-Muster usw.). Ergebnis wird dokumentiert (eigener Abschnitt in CLAUDE.md oder separate `CODING_GUIDELINES.md` — beim Start klären). Laufende, kollaborative Aufgabe.

- [ ] **Bestandscode nachtesten (= M2)** — bestehende Methoden sukzessive mit Unit Tests abdecken; ggf. kleine Refactorings für Testbarkeit (pure Logik aus Unity-Abhängigkeiten ziehen, analog `AudioFadeMath`). Vollständiges Methoden-Audit (Session 2026-06-08) in drei Gruppen nach Testbarkeit. Test-first für jede extrahierte Logik. Aufwand: L (laufend).
  - **✅ Gruppe A — pure/fast-pure, erledigt 2026-06-08** (14 Tests, alle mutation-geprüft):
    - `AudioManagerDictionaryProvider.FillLayerMaskDictionaryWithLayerRelatedValues` — 4 Tests (keep-first bei Duplikat-Layer).
    - `AudioManagerDictionaryProvider.FillDictionaryWithKeysAndValues` — 6 Tests (null/empty-Guards + skip-but-continue bei null-Eintrag + keep-first).
    - `WallLayerMask.FromLayers` — neu aus beiden WallCheck-Services extrahiert (pure Bitmask-Faltung), 4 Tests (OR-Faltung). Beide Services rufen jetzt den geteilten Helper.
  - **Gruppe B — echte Entscheidungslogik, Unity-gekoppelt → kleinen Seam VOR dem Test ziehen:**
    - [x] **`AudioPoolAcquisitionService.GetFreeAudioSourcePoolIndex` — erledigt 2026-06-15** (Commit `e0ac058`). „Slot frei?"-Prädikat in pure `PoolSlotAvailability.IsFree(isPlaying, currentTime, busyUntilTime, isPaused)` extrahiert (Unity-frei, nimmt rohe Werte statt `AudioObject`), 5 EditMode-Tests (je eine Klausel: silent+elapsed+unpaused=frei, playing=belegt, busy-window offen=belegt, paused=belegt trotz sonst-frei, `currentTime == busyUntil`=frei [inklusive Grenze]). `IsPaused`-als-belegt mit abgedeckt.
    - [x] **`ShouldContinueLoop` (beide WallCheck-Services) — erledigt 2026-06-21** (mit R3). OneShot/Loop/Paused-Fortsetzungsprädikat **plus neuer Generation-Guard** in pure `WallCheckContinuation.ShouldContinue(...)` extrahiert (Unity-frei, rohe Werte; 9 EditMode-Tests, mutation-geprüft). Beide Services rufen den geteilten Seam; `Source == null` + Editor-Warnung bleiben Unity-seitig.
    - [ ] `CalculateCutoffFrequency` — Akkumulation über den Hit-Buffer als Fold-Helper (Pro-Wand-Schritt ist via `WallOcclusionMath` schon getestet).
    - [ ] `AudioPlaybackService.Gate` + `ResolveVolume` — Gating (`CanHandleAudioSource`) bzw. Volume-Fallback-Entscheidung.
    - [ ] `AudioPauseService.PauseAll`/`UnpauseAll` — Scope-Logik (nur wecken, was wir pausiert haben); braucht Fake/Seam übers Pause-Primitiv.
  - **Gruppe C — dünne Glue/Orchestrierung & MonoBehaviour → PlayMode oder geringer Testwert:** `AudioManagerDynamic`-API (einzige echte Logik = Crossfade-Komposition), `AudioStopService.StopSlot`, `AudioFollowService.UpdateFollowers`, `AudioOcclusionSmoothingService.Tick`, WallCheck-Token-Lebenszyklus. Bündelt mit M3 + K1-PlayMode-Tests.

- [ ] **Coroutine-Variante gebündelt unter `!USE_UNITASK` gegenprüfen** — Änderungen an `AudioCoroutineWallCheckService` werden von Hand 1:1 zur UniTask-Version gespiegelt, bei aktivem UniTask aber NICHT mitkompiliert (`#if`). Statt jedes Mal umzuschalten, prüft Patrick solche Spiegelungen **gesammelt in einem Rutsch** (UniTask testweise aus → Compile + Coroutine-Smoke). **Offen seit 2026-06-08:** `GenerateLayerMaskFromDictionary` → `WallLayerMask.FromLayers`-Umstellung. **Dazu (2026-06-20):** W3-Umstellung — Ctor-Param `Transform _playerListener` → `IAudioListenerProvider _listenerProvider` und `CalculateCutoffFrequency` (`playerListener.position`/`== false` → `listenerProvider.TryGetPosition(out …)`) in *beiden* Services gespiegelt; bei aktivem UniTask kompiliert die Coroutine-Variante nicht mit → im Sammel-Check (UniTask aus) gegenprüfen. Künftige Doppel-Service-Änderungen hier anhängen, bis der Sammel-Check läuft. **Dazu (R7, Review 2026-06-21):** die Coroutine-Variante cached `AudioSource targetSource = poolArray[poolIndex].Source` einmal (~Z. 65) und prüft `targetSource == false`, während die UniTask-Variante `poolArray[poolIndex].Source` bei jedem Tick live liest — Mirroring-Drift (kein aktiver Bug, Pool-Source ist stabil). Im Sammel-Check angleichen. **Dazu (R3, 2026-06-21):** Generation-Guard-Spiegelung — `StartWallCheckLoop` cached `startGeneration = poolArray[poolIndex].Generation`, durchgereicht bis `ShouldContinueLoop`, das jetzt `WallCheckContinuation.ShouldContinue(...)` aufruft; in *beiden* Services gespiegelt → im Sammel-Check gegenprüfen. **Dazu (M4 + R4, 2026-06-21):** timeScale-Uhr-Tausch — `IsCurrentlyActive` und `ShouldContinueLoop`-`currentTime` auf `Time.unscaledTime` (M4) sowie der Intervall-Wait von `WaitForSeconds` → **`WaitForSecondsRealtime`** (Feld-Typ Z. 23 + Init Z. 43, R4); in *beiden* Services gespiegelt. **Im Sammel-Check besonders prüfen:** dass das gecachte `WaitForSecondsRealtime` bei Wiederverwendung sauber resettet (sonst per-Loop `new` statt cachen). **Mini-Cleanup beim Sammel-Check (Coroutine, da die Datei dann live kompiliert):** (a) totes Feld `pauseWait` (~Z. 24/42 — nur zugewiesen, nie gelesen; Rest eines alten Pause-Designs) entfernen; (b) optional **R8** (stale XML-Doc ~Z. 50–55, passt nicht zur `StartWallCheckLoop`-Signatur) gleich mit angleichen.

- [ ] **Pricing-Analyse** (wenn release-reif) — Asset-Store-Preise der Konkurrenz (MasterAudio & Co.) live benchmarken (WebSearch für aktuelle Zahlen), dann wertbasiert auf die Differenzierer ankern (saubere Architektur + Tests + lightweight Occlusion + Ease-of-use vs. FMOD). Finaler Preis ist Patricks Markt-Call; ich liefere Benchmark + Begründung, kein Verdikt.

---

# Erledigt

> Ledger abgeschlossener Aufgaben — **neueste zuerst.** Detaillierte Begründungen und Designentscheidungen leben in [`CLAUDE.md`](CLAUDE.md); hier nur **Was + Datum + Kürzel** (+ Folgearbeit-Pointer, falls offen).

### 2026-06-21
- **R3** — WallCheck-Schleife stoppt sich bei Slot-Reuse selbst (cached `Generation` beim Start, bricht bei fremder Generation ab); Fortsetzungs-Prädikat als pure `WallCheckContinuation` (9 EditMode-Tests). Generation-Guard bewusst **vor** dem Pause-Check. *Folgearbeit: Coroutine-Spiegelung → „Coroutine-Variante gegenprüfen" (Noch Offen).*
- **M4 + R4** — gesamtes Audio-Timing auf die **echte Uhr** entkoppelt von `Time.timeScale`: OneShot-Busy-Fenster auf `Time.unscaledTime` (eine Uhr, alle Vergleichsstellen konsistent; M4), WallCheck-Takt auf `DelayType.UnscaledDeltaTime`/`WaitForSecondsRealtime` (R4). **Contract:** Pausieren = `PauseAll()`/`UnpauseAll()`, **nicht** `timeScale=0`. Pure `PoolSlotAvailability`/`WallCheckContinuation` nehmen Zeit als rohe Zahl. *Folgearbeit: Coroutine-Spiegelung → „Coroutine-Variante gegenprüfen"; PlayMode-Smoke (beide Noch Offen).* ⚠️ *Fakt noch nicht in CLAUDE.md gespiegelt (timeScale-Contract).*
- **Doku: Audio-Timing an `PauseAll`, nicht `timeScale`** — Käufer-Contract-Hinweisblock unter „Pausieren / fortsetzen" in beiden User-Docs (DE+EN 1:1): Fades, Occlusion-Glides, OneShot-Slot-Lebenszeit und WallCheck laufen in realen Sekunden, von `timeScale` entkoppelt.

### 2026-06-20
- **W3** — Listener nicht mehr einmalig gecacht: beide WallCheck-Services halten `IAudioListenerProvider` statt roher `Transform`; `SceneAudioListenerProvider` cached + self-heilt bei jedem Zugriff (kein Polling), Resolve-Entscheidung pure in `ListenerCachePolicy` (3 EditMode-Tests). Fängt Respawn **und** Kamerawechsel per Disable/Enable ab. *Folgearbeit: PlayMode-Smoke + Coroutine-Spiegelung (beide Noch Offen).*
- **M1** — Fade-/Occlusion-Ticks auf `Time.unscaledDeltaTime` statt `Time.deltaTime`. Modell: Tool-Pause = `PauseAll`, nicht `timeScale`; Echtzeit-Animationen folgen der `AudioSource`-Logik (ignoriert `timeScale`). Konsumierende Mathe selbstklemmend (kein Overshoot/NaN bei Hitch-Spikes). *Folgearbeit: PlayMode-Smoke (Noch Offen).* ⚠️ *Fakt noch nicht in CLAUDE.md gespiegelt (unscaled-Tick-Modell).*
- **Multiplikativer (faktorbasierter) Frequenzabfall bei Occlusion** — Cutoff-Reduktion pro Wand multiplikativ: jeder Layer trägt Dämpfungsfaktor `0..1` (`WallOcclusionMath.ApplyWall = current − (current−floor)·d`, 9 EditMode-Tests). Reihenfolge-unabhängig, asymptotisch zum Floor. Datenfeld `WallDampingLayer.WallDampingFactor` (`[Range(0,1)]`), Array `WallDampingPerLayer`, `ConfigForTests` auf 0.45/0.65/0.82 migriert.

### 2026-06-04 (Code-Review-Härtung)
- **K1** — Singleton-`OnDestroy` nullt nicht mehr die lebende Instanz (`if (instance != this) return;`). *(Unit-Test folgt mit dem AudioManagerDynamic-Sammeltest + M3.)*
- **W2** — LowPass-Default 5000→22000 (transparent); `filter.enabled = UseWallCheck` pro Dispatch (`LowPassDispatchPolicy`, 4 Tests); Occlusion-Mathe in pure `WallOcclusionMath` (4 Tests), beide WallCheck-Services teilen den Seam. (Hz-Layer-Tuning später durch faktorbasiertes Modell ersetzt — siehe 2026-06-20.)
- **Occlusion-Glättung** (aus W2-Diskussion abgespalten) — WallCheck setzt nur noch `TargetCutoff`, neuer `AudioOcclusionSmoothingService` gleitet pro Frame (`OcclusionSmoothing`, 6 Tests); `OcclusionSmoothingSpeed` als Config-Feld. Kein „Pop" beim Aus-der-Wand-Treten.
- **W1 + P6** — `AudioHandle` trägt `Generation`; Stop/Fade prüfen `IsHandleCurrent` (Bounds + Generation, `AudioHandleValidator`, 6 Tests) → stale Handle = stilles No-op. `AudioHandle`-Ctor `internal` + `AudioHandle.Invalid` → P6 (selbstgebauter `{99999}`-Crash) strukturell zu.
- **P8** — CLAUDE.md auf IST-Zustand gebracht (API, `AudioCategory`, neue Services) + Memory-Wissen eingearbeitet.
